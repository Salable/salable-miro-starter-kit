import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The Miro panel lives at /app — allow that path to be served
  // without server-side auth redirects or other middleware interference.
};

export default nextConfig;
